function myMaths(first_num, second_num) {
  const sum = first_num + second_num;
  const rest = first_num - second_num;
  const multiply = first_num * second_num;
  const divide = first_num / second_num;
  console.log(sum, rest, multiply, divide);
}

myMaths(280, 29);
